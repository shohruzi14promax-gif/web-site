import { useEffect, useMemo, useState } from 'react';
import { Search, Users } from 'lucide-react';
import { getSiteData } from '../lib/supabase';
import { teachers as fallbackTeachers, type Teacher } from '../lib/data';
import { useI18n } from '../i18n';
export default function Teachers() { const { t } = useI18n(); const [items,setItems]=useState<Teacher[]>([]); const [query,setQuery]=useState(''); const [state,setState]=useState<'loading'|'success'|'empty'|'error'>('loading'); useEffect(()=>{let alive=true; getSiteData<Teacher[]>('teachers',[]).then(data=>{if(!alive)return;const records=Array.isArray(data)&&data.length?data:fallbackTeachers;setItems(records);setState(records.length?'success':'empty');}).catch(()=>{if(!alive)return;setItems(fallbackTeachers);setState(fallbackTeachers.length?'success':'error');});return()=>{alive=false};},[]); const filtered=useMemo(()=>{const q=query.trim().toLocaleLowerCase();return q?items.filter(x=>[x.name,x.subject,x.role,x.category].filter(Boolean).some(v=>String(v).toLocaleLowerCase().includes(q))):items},[items,query]); return <section id="teachers" className="scroll-mt-24 px-5 py-10 sm:px-8 lg:px-12 lg:py-16"><div className="mx-auto max-w-7xl"><div className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">{t('team')}</p><h2 className="mt-2 text-3xl font-black tracking-tight text-slate-950 dark:text-white sm:text-4xl">{t('teachers')}</h2></div>{state==='success'&&<label className="flex w-full max-w-sm items-center gap-2 rounded-2xl border border-slate-200 bg-white/90 px-4 py-3 shadow-sm dark:border-white/10 dark:bg-slate-900"><Search className="h-4 w-4 text-slate-400"/><span className="sr-only">{t('teacherSearchLabel')}</span><input aria-label={t('teacherSearchLabel')} value={query} onChange={e=>setQuery(e.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none dark:text-white" placeholder={t('teacherSearch')}/></label>}</div>{state==='loading'&&<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{[1,2,3,4].map(i=><div key={i} className="h-64 animate-pulse rounded-3xl border border-slate-200 bg-white/70 dark:border-white/10 dark:bg-slate-900/70"/>)}</div>}{state==='error'&&<div role="alert" className="mt-8 rounded-3xl border border-red-200 bg-red-50 p-6 text-sm text-red-700 dark:border-red-500/20 dark:bg-red-950/30 dark:text-red-300">{t('teacherError')}</div>}{state==='empty'&&<div className="mt-8 rounded-3xl border border-dashed border-slate-300 p-10 text-center dark:border-white/10"><Users className="mx-auto h-8 w-8 text-slate-400"/><p className="mt-3 font-semibold text-slate-700 dark:text-slate-200">{t('teacherEmpty')}</p></div>}{state==='success'&&<><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{filtered.map((teacher,index)=><article key={teacher.id||`${teacher.name}-${index}`} className="overflow-hidden rounded-[26px] border border-slate-200 bg-white shadow-sm dark:border-white/10 dark:bg-slate-900"><div className="aspect-[4/3] overflow-hidden bg-slate-100 dark:bg-slate-800">{teacher.image?<img src={teacher.image} alt={teacher.name} className="h-full w-full object-cover" loading="lazy" decoding="async"/>:<div className="flex h-full items-center justify-center text-slate-400"><Users className="h-10 w-10"/></div>}</div><div className="p-5"><p className="text-[11px] font-bold uppercase tracking-[.12em] text-blue-600">{teacher.subject||t('teachers')}</p><h3 className="mt-1 text-lg font-black text-slate-950 dark:text-white">{teacher.name}</h3><p className="mt-2 text-sm text-slate-600 dark:text-slate-300">{teacher.role}</p>{teacher.category&&<span className="mt-4 inline-flex rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600 dark:bg-slate-800 dark:text-slate-300">{teacher.category}</span>}</div></article>)}</div>{!filtered.length&&<div className="mt-5 rounded-2xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">{t('teacherNotFound')}</div>}</>}</div></section>; }
